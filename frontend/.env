# Local development environment for the BookStacks frontend.

# Where the Flask API is listening — the frontend proxies every /api/*
# request here.
BACKEND_URL=http://localhost:5000

# Port this Express server listens on.
PORT=3000
